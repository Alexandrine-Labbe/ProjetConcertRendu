{% extends 'base.html.twig' %}

{% block title %}{% trans %}Editer membre{% endtrans %}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-6">
            <h1>{% trans %}Editer membre{% endtrans %}</h1>
        </div>

        <div class="col-6 text-right">
            {{ include('app/member/_delete_form.html.twig') }}

            <a href="{{ path('member_index') }}" class="btn btn-primary text-white my-1">{% trans %}Retour à la liste{% endtrans %}</a>


        </div>
    </div>

    {{ include('app/member/_form.html.twig', {'button_label': 'Mettre à jour' | trans}) }}

{% endblock %}
