{% extends 'base.html.twig' %}

{% block title %}{% trans %}Liste des musiciens{% endtrans %}{% endblock %}

{% block body %}


    <div class="row">
        <div class="col-6">
            <h1>{% trans %}Liste des musiciens{% endtrans %}</h1>

        </div>

        <div class="col-6 text-right">
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('member_new') }}" class="btn btn-primary text-white">{% trans %}Créer{% endtrans %}</a>
            {% endif %}
        </div>
    </div>


    <div class="row">


        {% for member in members %}
            <div class="col-md-4 col-12 mb-3">
                <div class="card h-100">
                    <img src="{{ asset(member.picturePath) }}" class="card-img-top" alt="Photo membre">
                    <div class="card-body">
                        <h5 class="card-title"><a href="{{ path('member_show', {'id': member.id}) }}"> {{ member.name }} {{ member.firstName }}</a></h5>
                        <p class="card-text">{% trans %}Né le{% endtrans %} {{ member.birthDate |date('d-m-Y') }}</p>
                    </div>
                    <div class="card-footer">
                        {{ member.job }}
                    </div>
                </div>
            </div>


        {% else %}

            <div class="card">
                <div class="card-body">
                    {% trans %}Pas de musiciens existants.{% endtrans %}
                </div>
            </div>

        {% endfor %}
    </div>







    {{ knp_pagination_render(members) }}

{% endblock %}
