<footer class="footer bg-dark">
    <div class="container">
        <div class="text-white">Salle de concert : {{ concert.hall.name }}</div>
        <div class="text-white">Hall : {{ concert.hall.concertHall.name }}</div>
        <div class="text-white">Présentation : {{ concert.hall.concertHall.presentation }}</div>
        <div class="text-white">Ville : {{ concert.hall.concertHall.city }}</div>
    </div>
</footer>
