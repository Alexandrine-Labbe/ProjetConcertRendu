{% extends 'base.html.twig' %}

{% block title %}Editer utilisateur{% endblock %}

{% block body %}

    {% for flashMessage in app.session.flashbag.get('error') %}

        <div class="alert alert-danger" role="alert">
            {{ flashMessage }}
        </div>

    {% endfor %}




    <div class="row">
        <div class="col-6">
            <h1>Editer utilisateur</h1>
        </div>

        <div class="col-6 text-right">
            {{ include('app/user/_delete_form.html.twig') }}

            <a href="{{ path('user_index') }}" class="btn btn-primary text-white my-1">Retour à la liste</a>

        </div>
    </div>


    {{ include('app/user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}


{% endblock %}
