{% extends 'base.html.twig' %}


{% block title %}{% trans %}Modifier un concert{% endtrans %}{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-6">

            <h1>{% trans %}Modifier un concert{% endtrans %}</h1>

        </div>

        <div class="col-6 text-right">
            {{ include('app/concert/_delete_form.html.twig') }}

            <a href="{{ path('concert_index') }}" class="btn btn-primary text-white my-1">{% trans %}Retour à la liste{% endtrans %}</a>


        </div>
    </div>


    {{ include('app/concert/_form.html.twig', {'button_label': 'Mettre à jour' |trans }) }}


{% endblock %}
