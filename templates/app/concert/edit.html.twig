{% extends 'base.html.twig' %}


{% block title %}Modifier un concert{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-6">

            <h1>Modifier un concert</h1>

        </div>

        <div class="col-6 text-right">
            {{ include('app/concert/_delete_form.html.twig') }}

            <a href="{{ path('concert_index') }}" class="btn btn-primary text-white my-1">Retour à la liste</a>


        </div>
    </div>


    {{ include('app/concert/_form.html.twig', {'button_label': 'Mettre à jour'}) }}


{% endblock %}
